{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["APP_URL", "WEB_URL", "NEXT_PUBLIC_WEB_URL", "NODE_ENV"]
    },
    "dev": {
      "env": ["APP_URL", "WEB_URL", "NEXT_PUBLIC_WEB_URL", "NODE_ENV"]
    },
    "lint": {
      "env": ["APP_URL", "WEB_URL", "NEXT_PUBLIC_WEB_URL", "NODE_ENV"]
    }
  }
}