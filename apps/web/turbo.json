{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["APP_URL", "WEB_URL", "NEXT_PUBLIC_WEB_URL"]
    },
    "dev": {
      "env": ["APP_URL", "WEB_URL", "NEXT_PUBLIC_WEB_URL"]
    },
    "lint": {
      "env": ["APP_URL", "WEB_URL", "NEXT_PUBLIC_WEB_URL"]
    }
  }
}