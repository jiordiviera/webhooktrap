import { env } from '@/env'

export const siteUrl = env.app.url
export const apiUrl = env.api.url
export const productName = 'Hookscope'
export const productTagline = 'Webhook debugging, simplified.'
